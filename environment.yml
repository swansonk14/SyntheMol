name: SyntheMol

channels:
  - conda-forge
  - rdkit
  - pytorch

dependencies:
  - python>=3.10
  - gdown
  - matplotlib
  - numpy
  - pandas
  - pip
  - pytorch
  - rdkit
  - scikit-learn
  - scipy
  - tqdm
  - pip:
    - chemprop
    - descriptastorus
    - typed-argument-parser>=1.8.0

# TODO: make chem_utils on PyPI and then add it here
